# ASA Conecta - Agent Service (Python / FastAPI)
AGENT_SERVICE_HOST=0.0.0.0
AGENT_SERVICE_PORT=8000
AGENT_LOG_LEVEL=INFO
# Diretorio com agent.yaml, thresholds.yaml e rules.yaml (padrao: app/config)
AGENT_CONFIG_DIR=
